export const MotionConfig = {
  // Global speed multiplier for all idle motions
  globalSpeed: 0.3,

  // Transition Settings (For Phase 4)
  transitionDuration: 0.9,
  transitionEase: 'power3.inOut',
};
