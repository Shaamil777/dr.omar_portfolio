export const SystemConfig = {
  // Master toggles for motion systems.
  // Useful for isolating effects during tuning and debugging.
  ENABLE_IDLE: true,
  ENABLE_PARALLAX: true,
  ENABLE_CAMERA_DRIFT: true,
  ENABLE_TRANSITIONS: false, // Turned off for Phase 3
  
  DEBUG_MOTION: false, // Console logs or visual helpers
};
